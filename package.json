{
  "name": "apex-flow",
  "version": "1.0.0",
  "private": true,
  "description": "ApexFlow AI Document Processing Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "concurrently \"npm -w apps/api-gateway run start:dev\" \"npm -w apps/pdf-workflows run start:dev\" \"npm -w apps/slack-bot run dev\" \"npm -w apps/react-web run start\"",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test",
    "prepare": "husky install",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.2.1",
    "@typescript-eslint/parser": "^6.2.1",
    "concurrently": "^8.2.0",
    "eslint": "^8.46.0",
    "form-data": "^4.0.4",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3",
    "node-fetch": "^2.7.0",
    "prettier": "^3.0.1",
    "ts-node": "^10.9.1"
  },
  "lint-staged": {
    "*.{ts,js,json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
