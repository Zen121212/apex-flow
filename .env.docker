# Docker Container Environment Configuration
# Use this when running services in Docker containers

# Database (Docker service name)
MONGO_URI=mongodb://apexflow-mongo:27017/apexflow

# Queue & Cache (Docker service name)
REDIS_URL=redis://apexflow-redis:6379

# File Storage (MinIO - Docker service name)
S3_ENDPOINT=http://apexflow-minio:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=apexflow-local
S3_REGION=us-east-1

# Security
JWT_SECRET=changeme-super-secure-jwt-secret

# AI/ML Services
OPENAI_API_KEY=sk-placeholder-openai-key

# Slack Integration (do not commit real secrets)
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Service Ports (internal Docker network)
API_GATEWAY_PORT=3000
SLACK_BOT_PORT=3001
AGENT_ORCHESTRATOR_PORT=3002
ANGULAR_PORT=4200

# Environment
NODE_ENV=development

# OCR Service (Docker service name)
OCR_SERVICE_URL=http://apexflow-ocr-enhanced:3001

# Visual AI Service (Docker service name)
VISUAL_AI_SERVICE_URL=http://apexflow-visual-ai:8000

# Agent Orchestrator URL for inter-service communication
AGENT_ORCHESTRATOR_URL=http://apexflow-agent-orchestrator:3002
